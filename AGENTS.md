# AGENTS.md

Pointer file. The project context itself is **not** kept in this repository — it lives in the
sibling repo `meatloaf-agents` and is imported below. **Edit it there, not here.**

@../meatloaf-agents/meatloaf/AGENTS.md

---

If the import above did not resolve, you are seeing this note and nothing else — the project
instructions are missing. Clone the context repo as a sibling of this one; the import path is
relative, so it has to sit beside the repo rather than anywhere else:

```
source/
├── meatloaf/          <- you are here
└── meatloaf-agents/
```
