# CLAUDE.md

Claude Code looks for this filename, but the `lib/meatloaf` stream-architecture notes are kept in
`AGENTS.md` so they are shared with any assistant rather than tied to one tool. This file exists
only to bridge the two — it holds no content of its own.

@AGENTS.md

---

If the import above did not resolve, you are seeing this note and nothing else. `AGENTS.md` should
sit beside this file in `lib/meatloaf/` and holds the stream-architecture notes.
