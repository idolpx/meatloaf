# CLAUDE.md

Claude Code looks for this filename, but the project context is kept in `AGENTS.md` so it is shared
with any assistant rather than tied to one tool. This file exists only to bridge the two — it holds
no content of its own.

@AGENTS.md

---

If the import above did not resolve, you are seeing this note and nothing else. `AGENTS.md` should
sit beside this file at the repository root and holds the full project context.
