# AGENTS.md

Pointer file. The `lib/meatloaf` stream-architecture notes are **not** kept in this repository —
they live in the sibling repo `meatloaf-agents` and are imported below. **Edit them there, not
here.**

@../../../meatloaf-agents/meatloaf/lib/meatloaf/AGENTS.md

---

If the import above did not resolve, you are seeing this note and nothing else. Clone the context
repo as a sibling of the meatloaf repo; the import path is relative, so it has to sit beside it:

```
source/
├── meatloaf/          <- this file is at lib/meatloaf/AGENTS.md inside here
└── meatloaf-agents/
```
